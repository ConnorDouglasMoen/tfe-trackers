====================================================================
TFE TRACKERS - Reference Document
====================================================================
Project: tfe-trackers

--------------------------------------------------------------------
SYSTEM: Tales from Elsewhere (TFE)
--------------------------------------------------------------------

HP SYSTEM: Strain + Injuries
- Strain: integer 1-9. Survivors have strain 3 by default (max 9).
  Represented as checkboxes (filled = taken, shown as dark red X).
- Injuries: up to 3 types per character (Survivors have all 3):
    * Serious Injuries: up to 2 slots
    * Critical Injury:  1 slot
    * Lethal Injury:    1 slot
  Each injury slot has:
    - Description (text + CLEAR button)
    - Complications (text + CLEAR button; faded + strikethrough when Treated)
    - Treated toggle

CONDITIONS:
- Temporary statuses. Stored as string[]. Enter to add, X to delete.

CHARACTER TYPES:
- Survivors (Player Characters): strain 3, all injury types
- Other (NPCs): minimal defaults, +/- controls to add injury tiers
- Switching type preserves each blob independently (no data bleed)

--------------------------------------------------------------------
EXTENSION ARCHITECTURE
--------------------------------------------------------------------
Stack: React + TypeScript + Vite + Tailwind CSS + Zustand + MUI
OBR SDK: @owlbear-rodeo/sdk 3.1.0

Entry points (vite.config.ts multi-page):
  - index.html
  - src/background/background.html
  - src/tokenMenu/tokenMenu.html
  - src/action/action.html

Plugin ID prefix: com.tfe-trackers
Token metadata key:  "tokenRecord"
Hidden metadata key: "hidden"
Scene metadata key:  "sceneDisplaySettings"

--------------------------------------------------------------------
DATA MODEL
--------------------------------------------------------------------

--- Token metadata: TokenRecord ---

type CharacterType = "survivor" | "other";

type InjurySlot = {
  id: string;
  description: string;
  complications: string[];  // list, same pattern as conditions
  treated: boolean;
};

type CharacterData = {
  characterType: CharacterType;
  strainMax: number;         // 1-9
  strainCurrent: number;     // 0-strainMax
  hasSerious: boolean;
  seriousCount: 1 | 2;
  hasCritical: boolean;
  hasLethal: boolean;
  seriousInjuries: [InjurySlot, InjurySlot];
  criticalInjury: InjurySlot;
  lethalInjury: InjurySlot;
  conditions: string[];
};

type TokenRecord = {
  activeType: CharacterType;
  survivor: CharacterData;   // always preserved, even when "other" is active
  other: CharacterData;      // always preserved, even when "survivor" is active
};

Defaults:
  Survivor — strainMax:3, all injuries, seriousCount:2
  Other    — strainMax:1, hasSerious:true, seriousCount:1

Migration (migrateToTokenRecord):
  - Handles legacy CharacterData blobs (pre-TokenRecord)
  - Fills in characterType, seriousCount
  - Strips legacy displaySettings (now in scene metadata)
  - migrateInjurySlot: converts legacy single-string complications → string[],
                        migrates legacy `location` field → `description`
  - migrateCharacterData: also normalises legacy string conditions → string[]

--- Scene metadata: DisplaySettings ---

type DisplaySettings = {
  showStrain: boolean;
  showConditions: boolean;  // also gates complication bubbles on map
  // "all" = empty + filled circles; "filled-only" = filled only; "none" = hide row
  injuryDisplay: "all" | "filled-only" | "none";
  showName: boolean;        // gates displayName bubble on map
  markerScale: number;      // scales strain boxes + injury circles
  textScale: number;        // scales condition/complication/name bubbles
};

Stored at: OBR.scene.metadata[getPluginId("sceneDisplaySettings")]
Written by: GM only (Action panel)
Read by: all participants via OBR.scene.onMetadataChange
Defaults: showStrain:true, showConditions:true, injuryDisplay:"all",
          showName:true, markerScale:1, textScale:1

--- Per-token: TokenDisplayOverrides ---

type TokenDisplayOverrides = {
  showStrain: boolean | null;          // null = use scene setting
  showConditions: boolean | null;      // null = use scene setting
  injuryDisplay: "all"|"filled-only"|"none"|null; // null = use scene setting
};

Stored inside TokenRecord.displayOverrides on each token.
Editable by all users (players + GM) in the token context menu.
Resolution: resolveDisplaySettings(sceneSettings, tokenOverrides)
  - Non-null token value wins; null falls back to scene setting.
Default: all fields null (fully inherits from scene).

--------------------------------------------------------------------
ON-MAP DISPLAY
--------------------------------------------------------------------
Implemented in src/background/:
  onMapDisplay.ts   — init; fetches DPI via OBR.scene.grid.getDpi();
                      reads DisplaySettings from scene metadata;
                      full redraw on metadata change
  onMapHelpers.ts   — buildStrainItems, buildInjuryItems (takes
                      DisplaySettings arg), buildConditionItems
  onMapItemIds.ts   — deterministic ID generators + getAllAttachmentIds
  mathHelpers.ts    — getImageCenter (copied from owl-trackers)

Layout:
  Strain + Injury rows: rendered BELOW token (getTokenBottomLeft)
  Bubble row:           rendered ABOVE token (getTokenTopLeft)
    - Bubbles wrap to fit token width, left-aligned.
    - Width measured via canvas.measureText for accurate sizing.
    - Text bubble sizing comes from DisplaySettings.textScale.
  Name bubble:          rendered BELOW token, right-aligned near injury row
    - Uses TokenRecord.displayName.
    - Visibility gated by resolved DisplaySettings.showName.
    - Text/height sizing comes from DisplaySettings.textScale.
    - Vertical offset follows markerScale so it stays aligned with injury row.

  Row 1 (below): Strain boxes (13px squares, red; dark red X when filled)
  Row 2 (below): Injury circles (14px, color-coded by severity)
                   ○ = empty slot (shown only when injuryDisplay = "all")
                   ! = filled + untreated
                   ✓ = filled + treated
  Row 3 (above): Text bubbles — untreated complications + conditions
                   Both gated by showConditions (turning it off hides all bubbles)
                   Untreated complications: color = injury severity
                   Conditions:              white (#ffffff)
                   Dedup: same string from multiple sources → shown once,
                     color of highest severity (lethal > critical > serious > condition)
                   Background: #1a1a2e at 0.8 opacity; pill-shaped via buildPath

Injury circle colors:
  Serious  — fill #d68e68, stroke #a44f27
  Critical — fill #db7777, stroke #94272c
  Lethal   — fill #7c4291, stroke #501e64

Sizing:
  Scene-level controls live in Action.tsx as segmented S/M/L/XL controls:
    S = 0.75, M = 1, L = 1.25, XL = 1.5
  markerScale:
    - Applies to strain box size/gap/stroke/font.
    - Applies to injury circle size/gap/stroke/font.
  textScale:
    - Applies to condition/complication bubble height, padding, radius,
      stroke, font, and wrapping increments.
    - Applies to name bubble height, padding, radius, stroke, and font.
  onMapHelpers clamps both scale values to 0.5-2 as a defensive guard
  against malformed scene metadata.

DPI: fetched via OBR.scene.grid.getDpi() on each scene ready; cached
as module-level `sceneDpi` and passed to all builder helpers.

onMapDisplay.ts trigger for bubble row:
  Shows bubbles when showConditions=true AND:
    (any untreated injury has complications) OR (data.conditions.length > 0)

--------------------------------------------------------------------
DECISIONS MADE
--------------------------------------------------------------------
- Survivor/Other are fully independent blobs in TokenRecord; switching
  type only changes activeType, both blobs always preserved.
- DisplaySettings live in OBR scene metadata, not on tokens. GM-only
  UI in token menu. Changes affect all tokens for all participants.
- Strain boxes use inline styles (not Tailwind) to avoid purge issues.
- Strain default for Survivor: 3.
- "Open Full Editor" hidden when isPopover=true (?popover=1 URL param).
- Token overrides (TokenDisplayOverrides) stored in TokenRecord.displayOverrides.
  null = inherit from scene; non-null = override for that token only.
  resolveDisplaySettings() merges scene + token visibility overrides before building map items.
  markerScale/textScale are scene-level only and are not token-overridable.
- Bubble text width measured via canvas.measureText (not estimated).
- Tracked Token IDs stored in localStorage (key: getPluginId("trackedTokenIds")):
  - OBR player metadata is session-scoped and does not persist across refreshes.
    localStorage is private per-browser, persists indefinitely, synchronous.
  - Each user's list is private by virtue of being their own browser storage.
  - Context menu onClick in background.ts reads/writes localStorage directly.
  - Action panel store (useTrackedTokensStore) loads from localStorage on init
    (synchronous, no async wait) and listens for StorageEvent to catch
    cross-frame writes from the background script.
- writeTokenRecordToItem(itemId, record) added to itemMetadataHelpers.ts;
  used by TrackedTokenRow so each row writes directly to its target item
  without needing or affecting the current selection.
- TrackedTokenRow re-syncs local record from item prop on each items change
  so concurrent edits (player context menu + GM Action panel) stay in sync.
- Stale tracked token IDs are pruned automatically, not manually:
  useTrackedTokensStore.pruneStaleIds(liveItemIds) removes IDs absent from
  the live scene item list; called from Action.tsx in a useEffect on items
  change, for all roles (GMs and players both accumulate stale IDs when tokens
  are deleted). The prune is a no-op when nothing changes (no unnecessary
  localStorage writes or re-renders). Also covered by tests in
  useTrackedTokensStore.test.ts.

--------------------------------------------------------------------
FILE MAP
--------------------------------------------------------------------
src/
  characterDataHelpers.ts     — types, factories, guards, migration
  tokenRecordMutations.ts     — pure mutation helpers for TokenRecord/CharacterData;
                                used by useCharacterDataStore and TrackedTokenRow
  itemMetadataHelpers.ts      — read/write TokenRecord + hidden flag;
                                getActiveDataFromItem
  useCharacterDataStore.ts    — Zustand store for TokenRecord
  useSceneDisplayStore.ts     — Zustand store for scene DisplaySettings;
                                init() wires OBR.scene.onMetadataChange
  useTrackedTokensStore.ts    — Zustand store for per-user pinned token IDs;
                                init() wires OBR.player.onChange listener;
                                trackToken/untrackToken write to localStorage;
                                pruneStaleIds() removes deleted-token IDs
  useOwlbearStore.ts
  useOwlbearStoreSync.ts
  getPluginId.ts / getTheme.ts / OBRThemeProvider.tsx
  index.css / lib/utils.ts

  background/
    background.html
    background.ts             — separate player/GM embed heights;
                                calls initOnMapDisplay()
    mathHelpers.ts
    onMapDisplay.ts           — fetches DPI; reads scene DisplaySettings
    onMapHelpers.ts           — builders; injuryDisplay + sizing from DisplaySettings
    onMapItemIds.ts

  tokenMenu/
    tokenMenu.html / main.tsx / App.tsx
    TokenMenu.tsx             — TokenDisplaySection (exported; all users): per-token
                                overrides for Strain/Injuries/Conditions;
                                Scene = inherit, Show/Hide or All/Filled/None = override

  action/
    action.html / main.tsx / App.tsx
    Action.tsx                — GM-only: On-Map Display settings (scene-level),
                                including marker/text sizing controls
    TrackedTokenRow.tsx       — collapsible per-token editor row;
                                manages own TokenRecord state;
                                writes to OBR by item ID (not selection)

  components/
    PartiallyControlledInput.tsx / TextInput.tsx / ToggleButton.tsx
    StrainRow.tsx
    InjurySlotCard.tsx  — complications as string[]; Enter to add, X to delete
                          Treated auto-resets when description+complications cleared
    TokenEditor.tsx     — shared editor fragment: Survivor/Other toggle,
                          strain, injuries, conditions. Props-based; no store
                          coupling. Also exports: Stepper, ConditionsEditor.
                          headingLevel prop: "h2" (TokenMenu) / "h3" (TrackedTokenRow).
                          topRowExtra slot: TokenMenu passes GM "Open Editor" button.

  test/
    characterDataHelpers.test.ts   — types, factories, guards, migration,
                                     constants, resolveDisplaySettings
    tokenRecordMutations.test.ts    — all pure mutation helpers: strain clamping,
                                     type switching, conditions, injury slots,
                                     seriousTier stepper, displayName, immutability,
                                     cross-blob isolation
    itemMetadataHelpers.test.ts    — read/write/clear metadata; hidden flag;
                                     legacy key fallback; new-key-wins precedence
    mathHelpers.test.ts            — getImageCenter (scale, rotation, DPI,
                                     offset, identity, negative scale cases)
    onMapHelpers.test.ts           — buildStrainItems, buildInjuryItems,
                                     buildConditionItems, buildNameBubble;
                                     survivor vs other seriousCount;
                                     dedup / severity color precedence;
                                     scale clamping; output shapes/colors/IDs
    onMapDisplay.test.ts           — display-layer gating for injuryDisplay
                                     and showConditions before builder helpers
    onMapItemIds.test.ts           — all ID generators; getAllAttachmentIds
                                     count, uniqueness, and prefix checks
    setup.ts                       — OBR SDK mock; localStorage mock;
                                     canvas.measureText mock
    useCharacterDataStore.test.ts  — all store mutations: strain clamping,
                                     type switching, injury toggles,
                                     add/removeCondition, display overrides,
                                     displayName, writeToItem callback
    useSceneDisplayStore.test.ts   — initial state; setSettings patches;
                                     OBR write-through; init() metadata load;
                                     onMetadataChange reactive updates
    useTrackedTokensStore.test.ts  — localStorage persistence; track/untrack;
                                     StorageEvent sync; malformed data resilience;
                                     pruneStaleIds: removes stale IDs, persists
                                     to storage, no-op when nothing changes
    TrackedTokenRow.test.tsx       — applyMutation plumbing, record re-sync from
                                     item prop, commitName (Enter, item.name match,
                                     pre-fill), cancelEditName (Escape + button),
                                     unpin button
    TokenMenu.test.tsx             — handleAdjustSeriousTier adapter (all 6 tier
                                     transitions); TokenDisplaySection RTL:
                                     collapse/expand, showStrain/showConditions/
                                     injuryDisplay/showName buttons — active class
                                     + store write; pre-seeded store state
    useOwlbearStoreSync.test.ts    — scene ready/not-ready lifecycle; items
                                     subscription (getItems, onChange, unsub,
                                     clear on not-ready); stale-promise guard;
                                     player sync (role, selection, onChange);
                                     theme sync (getTheme, onChange)

public/
  manifest.json               — plugin icon: tales-from-elsewhere-logo.svg
                                action icon: tfe-cowboy.svg
  manifest.dev.json           — generated local-dev manifest with localhost
                                URLs; created by npm run manifest:dev
  tales-from-elsewhere-logo.svg
  tfe-cowboy.svg              — used for both Action button AND context menu icon

scripts/
  write-dev-manifest.mjs      — reads public/manifest.json and writes
                                public/manifest.dev.json with absolute
                                local URLs for Owlbear development

--------------------------------------------------------------------
DEV TESTING FLOW
--------------------------------------------------------------------
Local Owlbear development:
  1. Run:
       npm run dev:obr
  2. Install/load this manifest URL in Owlbear:
       http://localhost:5173/manifest.dev.json
  3. Keep the Vite server running while testing.

Scripts:
  npm run manifest:dev
    - Generates public/manifest.dev.json.
    - Default base URL: http://localhost:5173
    - Optional custom base:
        node scripts/write-dev-manifest.mjs --base-url http://localhost:3000

  npm run dev:obr
    - Runs manifest:dev, then starts Vite with:
        vite --host 0.0.0.0 --port 5173

Manual QA checklist for local Owlbear testing:
  - Install DEV manifest and confirm TFE Trackers appears in Owlbear.
  - Open a scene as GM; confirm Action panel loads.
  - Right-click a CHARACTER or MOUNT image token; confirm TFE Trackers menu.
  - Edit Survivor strain, injuries, complications, treated state, and conditions.
  - Switch to Other; confirm Survivor data is preserved separately.
  - Set token display name; confirm bottom-right name bubble renders.
  - Toggle scene display settings and marker/text sizing from Action panel.
  - Set token display overrides from token menu and confirm they win over scene settings.
  - Pin token to Action panel; edit it from tracked row without changing selection.
  - Delete a tracked token and confirm it is removed from the tracked list.
  - Test as PLAYER where possible; confirm GM-only controls are unavailable.

--------------------------------------------------------------------
TODO / OPEN ITEMS
--------------------------------------------------------------------
- Add User Guide
- Consider using Red's Wound Markers for injuries.

--------------------------------------------------------------------
AUDIT: REMAINING TECHNICAL TASKS (2025-06)
--------------------------------------------------------------------

-- BUG / CORRECTNESS --

1. [DONE] `useOwlbearStoreSync` stale promise fix: added `cancelled` flag in the
   items effect so a getItems() promise that resolves after cleanup (scene went
   not-ready mid-fetch) cannot overwrite the already-cleared items[] state.
   File: src/useOwlbearStoreSync.ts

2. [WONTFIX] `getChangedItems` does not track rotation — on-map display items
   do not rotate with their token. This is acceptable behaviour by design.
   File: src/background/onMapDisplay.ts

3. [DONE] All four `updateItems` calls in `itemMetadataHelpers.ts` now awaited:
   `writeTokenRecordToItem`, `writeTokenRecordToSelection`, `clearTokenData`,
   and `writeHiddenToSelection`.
   File: src/itemMetadataHelpers.ts

-- CORRECTNESS / DATA --

4. [DONE] `createInjurySlot` now uses `crypto.randomUUID()` for slot IDs.
   File: src/characterDataHelpers.ts

5. [DONE] `applySetDisplayName` now trims whitespace before storing. Two new
   tests added covering the trim and whitespace-only cases.
   Files: src/tokenRecordMutations.ts, src/test/tokenRecordMutations.test.ts

6. [WONTFIX] `MAX_CONDITIONS = 20` bubble slot cap is sufficient in practice —
   accumulating more than 20 conditions + complications on a single token is
   not an expected use case.
   File: src/background/onMapItemIds.ts

-- TESTS --

7. [DONE] RTL component tests written for `TrackedTokenRow`: applyMutation
   plumbing, record re-sync from item prop, commitName (Enter, matching
   item.name, pre-fill), cancelEditName (Escape + cancel button), and unpin
   button. `@testing-library/react` and `@testing-library/user-event` added
   as devDependencies (requires `pnpm install`).
   Files: src/test/TrackedTokenRow.test.tsx, package.json

8. [DONE] Tests written for `TokenMenu` / `TokenDisplaySection`:
   - `handleAdjustSeriousTier` adapter: all 6 tier transitions (0→1, 1→2,
     clamped at 2; 2→1, 1→0, clamped at 0) tested via store directly.
   - `TokenDisplaySection` RTL tests: collapse/expand toggle; showStrain,
     showConditions, injuryDisplay (all 4 options), and showName button
     states — active class reflects current store value; clicking each
     button writes the correct override to the store.
   `TokenDisplaySection` exported from `TokenMenu.tsx` to enable isolation.
   Files: src/test/TokenMenu.test.tsx, src/tokenMenu/TokenMenu.tsx

9. [DONE] `onMapDisplay.test.ts` extended with three new describe blocks:
   - generation-counter abort: stalls the first refreshAll at getDpi via a
     hanging promise, fires a second refreshAll via onReadyChange(true), lets
     the second complete, then resolves the stall — asserts deleteItems is
     called exactly once (stale run aborted after its first await).
   - sceneListenersSet guard: fires onReadyChange(true) a second time after
     init; asserts items.onChange subscription count is unchanged (no
     double-subscribe).
   - not-ready teardown: seeds items.onChange with a spy unsubscribe fn,
     fires inner onReadyChange(false), asserts unsub called; then fires
     onReadyChange(true) and asserts items.onChange re-subscribed.
   Helper `initAndCaptureAllListeners` added: captures both onReadyChange
   registrations (outer top-level + inner startListeners), plus onItemsChange
   and onMetaChange.
   File: src/test/onMapDisplay.test.ts

10. [DONE] `useOwlbearStoreSync` tests written:
    - Scene ready/not-ready lifecycle: isReady() true/false on mount;
      onReadyChange fires true/false.
    - Items subscription: getItems called + items set when ready;
      onChange registered; onChange callback updates store; items cleared
      to [] on not-ready; unsubscribe called on not-ready; getItems not
      called when sceneReady=false.
    - Stale-promise guard (item 1 regression): getItems resolves after
      sceneReady goes false — stale result must NOT overwrite the [] already
      set by the not-ready transition.
    - Player sync: getRole/getSelection on mount; onChange updates role +
      selection; onChange subscribed.
    - Theme sync: getTheme on mount; onChange updates themeMode; onChange
      subscribed.
    OBR mock in setup.ts patched to add getRole and type-annotate
    getSelection return value.
    Files: src/test/useOwlbearStoreSync.test.ts, src/test/setup.ts

-- UX / POLISH --

11. [DONE] The "Pin to Action Panel" context menu label is static — it always says
    "Pin" even when the token is already pinned. OBR does not support dynamic
    label changes (static registration), but the label could read
    "Pin / Unpin from Action Panel" to signal toggle behaviour.
    File: src/background/background.ts

12. [DONE] Action panel width is fixed at 300px in `manifest.json`. The tracked
    token list can become crowded with many tokens. Consider whether 340-360px
    would be a better default (purely cosmetic).
    File: public/manifest.json

13. [WONTDO] `TrackedTokenRow` collapses on unpin but doesn't animate height —
    the row disappears abruptly. A brief CSS transition would improve the feel.

-- DEPENDENCY / BUILD --

14. [DONE] `@mui/material` and `@emotion/*` are listed as production dependencies
    but are not imported anywhere in the current codebase (grep confirms no MUI
    usage). These add ~300 kB to the bundle unnecessarily and should be removed.
    File: package.json

15. [WONTDO] `manifest.json` sets `"action"."height": 200` as the initial height,
    but `Action.tsx` calls `OBR.action.setHeight` dynamically via ResizeObserver.
    The initial 200px causes a visible jump on open. Setting it to a reasonable
    heuristic (e.g. 400) or the typical minimum would reduce flicker.
    File: public/manifest.json

16. [DONE] `getFillPortion` and `createRoundedRectangle` were already removed
    from `mathHelpers.ts` (resolved between sessions). The file now only exports
    `getImageCenter`. The test file already only tests `getImageCenter`.
    File: src/background/mathHelpers.ts

-- SECURITY / ROBUSTNESS --

17. [DONE] `getTokenRecordFromSelection` now requires an explicit `items: Item[]`
    parameter — the optional fallback `OBR.scene.items.getItems()` call has been
    removed. `TokenMenu` already passed items (no call-site change needed).
    Test updated: removed the "no args" test; added assertion that `getItems`
    is NOT called when items are provided.
    Files: src/itemMetadataHelpers.ts, src/test/itemMetadataHelpers.test.ts